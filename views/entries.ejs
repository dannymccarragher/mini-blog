<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/entries.css">
</head>
<body>
    <h1>All Posts</h1>
    <% if (posts.length !== 0 ) { %>
    <%  for (let i = 0; i < posts.length; i++){ %>
        <div>
               <h3> <%= posts[i].title %> </h3>
                
            <p><%= posts[i].content %></p>
            <p>Author:
                <!-- Say no author instead of null   -->
                <% if (!posts.author) { %>
                    No author
                <% } else { %>
                    <%= posts.author %>
                <% } %>
            </p>
            
            
    </div>
        <% } %>
        <% } else { %>
            <h1 id="no-post-header">No Posts!</h1>
            <% } %> 
</body>
</html>